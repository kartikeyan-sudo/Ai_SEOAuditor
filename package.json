{
  "name": "ai-seo-auditor",
  "version": "1.0.0",
  "description": "AI-assisted deterministic SEO auditing platform",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install"
  }
}
